# ── 원자료 ────────────────────────────────────────────────
# 국민건강영양조사 SPSS 원본과 배포 zip. 375MB이고 바뀌지 않는다.
# 파일은 디스크에 그대로 있고 git만 추적하지 않는다.
rank/raw/

# ── 백업 ──────────────────────────────────────────────────
# 2026-09-21 파일명 정리 전 스냅샷(15.8MB). 앞으로는 git 이력이 이 역할을 한다.
outputs/_archive/_backup/

# ── 작업 중 생기는 것 ─────────────────────────────────────
.tmp_artifact/
새 폴더/

# ── 프로그램이 만드는 임시 파일 ───────────────────────────
~$*
*.tmp
Thumbs.db
desktop.ini
.DS_Store
